saas-erp/

# Secrets — never commit real env files (keep .env.example)
.env
.env.*
!.env.example

# Database dumps / exports — contain real data (PII, password hashes); never commit.
# Patterns are scoped so tracked schema/seed files (schema.sql, sample-data.sql,
# create_*.sql, migration_*.sql) are unaffected.
database/*ecosudar*.sql
database/*dump*.sql
database/*backup*.sql
*.sql.gz
api/../fast25sms.txt
fast25sms.txt

# Build / tool artifacts
frontend/dist/
frontend/dist-dealer/
frontend/graphify-out/
pdf-template-studio/out/
node_modules/

# Generated Hostinger deploy bundles
/*.com/

# Editor deploy configs with credentials — never commit
.vscode/sftp.json
sftp.json
**/sftp.json

offline_timesfm/
create_offline_timesfm.py
offline_timesfm.zip
offline_timesfm.zip.part00
offline_timesfm.zip.part01
.vscode/
.codex/
.agents/
